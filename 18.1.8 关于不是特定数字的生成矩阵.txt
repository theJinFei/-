1： 需要将矩阵做截断处理，（注意，这个截断是由标准的任意选取多少行，多少列来进行的），
	即生成矩阵可能是不一样的，但最后的检验能力 一定是一样的
2： 对于下标的处理，从后到前编号或者从前到后进行编号，对结果是不影响的，但编码的结果可能是不同的，比如说bitset这个对象
	b.test(pos)是测试在pos的位置b的这个位是否为1
	生成矩阵 用matlab生成即可 即 (X^n-i) % g(x),用的是模2除法
3：	H' 即可以生成矩阵的转置，inv(H)生成矩阵的逆，当生成矩阵的逆的时候，要求H为方阵
4：	eye(N) 生成 N * N的单位矩阵	
	
	
	